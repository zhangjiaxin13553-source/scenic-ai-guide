# Online demo dependencies (Render free tier, Python 3.12)
# torch 由 render.yaml 的 buildCommand 单独装 CPU 版（避免拉取 ~2GB 的 CUDA wheel，
# 免费 512MB 实例也放不下）；sentence-transformers 运行时未被 import，故不列出。

chromadb==1.5.9
transformers==5.8.1
jieba==0.42.1
requests==2.32.3
python-dotenv==1.2.2
tqdm==4.67.3
numpy==2.2.0
huggingface_hub==1.28.0
gradio==6.22.0
